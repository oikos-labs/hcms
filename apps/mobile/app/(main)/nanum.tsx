import { Text, View } from "react-native";

export default function NanumScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="font-brand text-display text-text-heading">나눔</Text>
    </View>
  );
}
